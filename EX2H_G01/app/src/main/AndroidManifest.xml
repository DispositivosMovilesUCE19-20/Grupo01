<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.login">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-feature android:name="android.hardware.camera.flash" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name="ec.edu.uce.optativa3.controlador.viewMenuSensores" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="ec.edu.uce.optativa3.controlador.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name="ec.edu.uce.optativa3.controlador.SensorLuz" />
        <activity android:name="ec.edu.uce.optativa3.controlador.activity_form" />
        <activity android:name="ec.edu.uce.optativa3.controlador.item_list_data" />
        <activity android:name="ec.edu.uce.optativa3.controlador.guardarActivity" />
        <activity android:name="ec.edu.uce.optativa3.controlador.Inicio" />
        <activity android:name="ec.edu.uce.optativa3.controlador.Mostrar" />
        <activity android:name="ec.edu.uce.optativa3.controlador.Registrar" />
        <activity android:name="ec.edu.uce.optativa3.controlador.RegistroUsuario" />
        <activity android:name="ec.edu.uce.optativa3.controlador.mostrarLog" />
        <activity android:name="ec.edu.uce.optativa3.controlador.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="ec.edu.uce.optativa3.controlador.ServicioLog"
            android:enabled="true" />
        <receiver android:name="ec.edu.uce.optativa3.controlador.BroadcastNotify"></receiver>
    </application>

</manifest>